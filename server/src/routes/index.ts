import { Express } from 'express';

export function setupRoutes(app: Express) {
  // Add any route setup here
  // For now, we'll just log that routes are being set up
  console.log('Setting up routes...');
} 